[package]
name = "app_host"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
egui = "0.34"
# The wire contract shared with the guest SDK (osvauld-app).
app_abi = { path = "../app_abi" }
# Runs the sandboxed app modules.
wasmtime = "44"
