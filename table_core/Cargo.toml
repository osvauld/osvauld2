[package]
name = "table_core"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
loro = "1.12.0"
# Exact base-10 money: a Decimal cell is stored in Loro as a string (exactness over the wire),
# projected back to Decimal via the schema. Floats never touch money.
rust_decimal = "1.42"
